#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../css2stylus/bin/css2stylus" "$@"
  ret=$?
else 
  node  "$basedir/../css2stylus/bin/css2stylus" "$@"
  ret=$?
fi
exit $ret
